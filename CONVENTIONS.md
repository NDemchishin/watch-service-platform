# Конвенции проекта watch-service-platform

> Этот файл — обязательное чтение для любого AI-агента перед внесением изменений.
> Нарушение этих правил = регрессия. Все правила имеют одинаковый приоритет.

---

## 1. Архитектура

### 1.1. Разделение бот/бэкенд
- Бот **НИКОГДА** не обращается к БД напрямую. Только через FastAPI API.
- Backend и бот — раздельные приложения с чётким контрактом через HTTP.
- Все новые данные для бота → новый API-эндпоинт → вызов из `api_client.py`.

### 1.2. Event sourcing
- **НИЧЕГО не перезаписывается.** Любое действие = новая запись в `history_events`.
- **НИЧЕГО не удаляется.** Сотрудники деактивируются (soft delete), данные остаются навсегда.
- Каждый history event содержит: тип, payload, datetime, telegram_id, telegram_username.

### 1.3. Расширяемость типов работ
- **ЗАПРЕЩЕНО** хардкодить логику под конкретные типы работ (сборка, механизм, полировка).
- Новые типы работ должны добавляться без изменения существующего кода.
- Типы работ хранятся в БД (таблица `operation_types`), не в коде.

---

## 2. Telegram-бот (UX)

### 2.1. Навигация
- Кнопка **"Назад"** ВСЕГДА возвращает на **предыдущий шаг**, а не в главное меню.
- Для возврата в главное меню есть отдельная кнопка **"🏠 В меню"**.
- Реализация: навигационный стек в FSM data (`nav_history`).
- На каждом шаге multi-step flow должны быть ОБЕ кнопки: "⬅ Назад" и "🏠 В меню".

### 2.2. Опциональные поля
- Если поле опционально — ОБЯЗАТЕЛЬНА кнопка **"⏭ Пропустить"**.
- **ЗАПРЕЩЕНО** требовать ввод символа "-" или любого placeholder для пропуска.
- Использовать единый хелпер `get_optional_input_keyboard(field, back_to)`.

### 2.3. Подтверждения
- Деструктивные действия (деактивация сотрудника, изменение дедлайна, возврат) требуют **двухшагового подтверждения**.
- В подтверждении показывать **конкретные данные** (имя сотрудника, номер квитанции), а не абстрактный вопрос.
- **ЗАПРЕЩЕНО** показывать ID вместо имён в пользовательском интерфейсе.

### 2.4. Управление состояниями (FSM)
- **ЗАПРЕЩЕНО** делать `state.clear()` при ошибке. Вместо этого — повторный запрос текущего шага.
- `state.clear()` допустим ТОЛЬКО при завершении flow или по команде `/cancel`.
- Глобальный `/cancel` хендлер ОБЯЗАН работать из любого состояния.
- При каждом `callback_query` ОБЯЗАТЕЛЬНО вызывать `await callback.answer()`.

### 2.5. Сообщения
- Бот **редактирует существующее сообщение**, а не отправляет новое (одно-экранный подход из ТЗ).
- Исключение: сообщения об ошибках и уведомления.

---

## 3. Backend API

### 3.1. Аутентификация
- ВСЕ эндпоинты ОБЯЗАНЫ быть защищены через `Depends(verify_api_key)`.
- API Key передаётся через header `X-API-Key`.
- Telegram webhook защищён через `X-Telegram-Bot-Api-Secret-Token`.

### 3.2. Транзакции
- `get_db()` dependency оборачивает запрос в транзакцию (commit на успех, rollback на ошибку).
- В сервисах использовать `db.flush()`, а не `db.commit()`.
- Многошаговые операции (создание + история + нотификация) — в рамках ОДНОЙ транзакции.

### 3.3. Валидация
- ВСЕ FK (employee_id, operation_type_id, polisher_id, receipt_id) ОБЯЗАНЫ проверяться на существование перед использованием.
- Даты дедлайнов — только в будущем.
- `returned_at` — только после `sent_at`.
- Дубликаты (receipt_number, telegram_id) — ловить через `IntegrityError`, не через check-then-insert.

### 3.4. Пагинация
- ВСЕ list-эндпоинты ОБЯЗАНЫ поддерживать `skip`/`limit`.
- `total` в ответе = `SELECT COUNT(*)` (реальное количество в БД), не `len(items)`.
- Использовать единый `PaginatedResponse[T]`.

### 3.5. Обработка ошибок
- Единая иерархия исключений: `AppException` → `NotFoundException`, `ValidationException`, `DuplicateError`.
- Глобальный `@app.exception_handler(AppException)`.
- Стандартный формат ответа: `{"detail": "...", "error_code": "..."}`.
- **ЗАПРЕЩЕНО** возвращать стектрейсы в production.

### 3.6. HTTP статус коды
- 200 — успех (get, update)
- 201 — создано (create)
- 400 — ошибка валидации
- 404 — ресурс не найден
- 409 — конфликт (дубликат)
- 500 — внутренняя ошибка (только с generic-сообщением)

---

## 4. Безопасность

### 4.1. SQL-инъекции
- **ВСЕ** запросы к БД — ТОЛЬКО через SQLAlchemy ORM с параметризацией.
- **ЗАПРЕЩЕНО:** `text(f"...")`, `session.execute(f"...")`, `.filter(f"...")`, конкатенация строк в запросах.
- **ЗАПРЕЩЕНО:** динамические имена таблиц/колонок из пользовательского ввода.
- **ОБЯЗАТЕЛЬНО:** все входные данные через Pydantic-схемы.

### 4.2. Конфигурация
- **ЗАПРЕЩЕНЫ** дефолтные значения для секретов (DATABASE_URL, BOT_TOKEN, API_KEY).
- Если переменная окружения не задана — приложение НЕ стартует.
- `DEBUG` по умолчанию `False`.
- CORS — ограничен конкретными доменами (не `"*"`).

### 4.3. Данные
- Пользовательский ввод (комментарии, имена) — санитизация перед сохранением.
- Telegram-метаданные (telegram_id, username) — передавать в JSON body, НЕ в URL-параметрах.

---

## 5. Datetime и таймзоны

### 5.1. Единое правило: Moscow Time
- **ЗАПРЕЩЕНО:** `datetime.utcnow()`, `datetime.now()` без таймзоны, naive datetime.
- **ОБЯЗАТЕЛЬНО:** `datetime.now(MOSCOW_TZ)` через хелпер `now_moscow()` из `core/utils.py`.
- В моделях: `Column(DateTime(timezone=True), default=lambda: now_moscow())`.
- PostgreSQL: `SET timezone = 'Europe/Moscow'`.
- Для пользователя: единый форматтер `format_datetime(dt) -> str` с форматом `"дд.мм.гггг чч:мм"`.

### 5.2. Конфигурация
```python
# core/config.py
from zoneinfo import ZoneInfo
MOSCOW_TZ = ZoneInfo("Europe/Moscow")

# core/utils.py
from datetime import datetime
from .config import MOSCOW_TZ

def now_moscow() -> datetime:
    return datetime.now(MOSCOW_TZ)

def format_datetime(dt: datetime) -> str:
    return dt.astimezone(MOSCOW_TZ).strftime("%d.%m.%Y %H:%M")
```

---

## 6. Качество кода

### 6.1. Логирование
- Каждый сервис ОБЯЗАН иметь `logger = logging.getLogger(__name__)`.
- Все create/update/delete — `logger.info(...)`.
- Все ошибки — `logger.exception(...)` (с полным стектрейсом).
- **ЗАПРЕЩЕНО:** `print()`, голый `except:`, `except Exception: pass`.

### 6.2. Naming
- Модели: `CamelCase` (Receipt, Employee, PolishingDetails)
- Таблицы: `snake_case` (receipts, employees, polishing_details)
- API пути: `kebab-case` (/assign-master, /initiate-return)
- Схемы: `ModelCreate`, `ModelResponse`, `ModelUpdate`
- Сервисы: `ModelService` (ReceiptService, EmployeeService)

### 6.3. Тестирование
- Новые эндпоинты и сервисы ОБЯЗАНЫ иметь тесты.
- Тесты API: через FastAPI TestClient.
- Тесты бота: с мок API-клиентом.

### 6.4. Производительность
- **ЗАПРЕЩЕНЫ** N+1 запросы. Использовать JOIN / GROUP BY / subquery.
- Connection pooling: `pool_size=10`, `max_overflow=20`, `pool_pre_ping=True`.
- FK-колонки ОБЯЗАНЫ иметь индексы.

---

## 7. Чеклист перед коммитом

Перед каждым коммитом проверить:

- [ ] Все новые эндпоинты защищены аутентификацией
- [ ] Все FK проверяются на существование
- [ ] Все list-эндпоинты имеют пагинацию
- [ ] Все даты через `now_moscow()`
- [ ] Все callback_query отвечают `await callback.answer()`
- [ ] Кнопка "Назад" ведёт на предыдущий шаг (не в меню)
- [ ] Опциональные поля имеют кнопку "Пропустить"
- [ ] Деструктивные действия имеют подтверждение
- [ ] Нет `state.clear()` в обработчиках ошибок
- [ ] Нет raw SQL с пользовательским вводом
- [ ] Есть логирование операций
- [ ] Есть тесты на новый код
